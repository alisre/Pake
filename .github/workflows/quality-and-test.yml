name: Quality & Testing

# Disabled by request: this workflow's jobs will always be skipped.
# Keep the file so it can be re-enabled easily later.

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:

jobs:
  _disabled:
    name: Disabled
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Quality & Testing workflow is disabled."
